// Ejemplo 2: for Each para calcular la suma de todos los elementos de una lista
let sum = 0;
const numbers2 = [1, 2, 3, 4, 5];
numbers2.forEach(num => sum += num) // el ciclo foreach ejecutara la misma sentencia para cada uno de los elementos que la conforman
console.log("Ejemplo 2: Cálculo de la suma de los elementos de la lista")
console.log(sum)
